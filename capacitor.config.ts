import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angionic_Movies',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
